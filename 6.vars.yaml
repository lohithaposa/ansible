- name: files in the variables
  hosts: local
  connection: yes
  vars_files:  #from course.yaml file you are loading the variables.
  - course.yaml
  tasks:
  - name: print variables
    vars:
      Course: Ansible
    ansible.builtin.debug:
      msg: "Course is: {{ COURSE }}, Trainer is: {{ TRAINER }}" 

