- name: variable preferences
  hosts: local
  connection: local
  # vars:
  #   NAME: "Ansible from Play level"
  # vars_files:
  #   - course.yaml
  vars_prompt:
  - name: username 
    prompt: Please enter username
    private: false
  tasks:
  - name: Task level variable
    # vars:
    #  Course: "Ansible from task level"
    ansible.builtin.debug:   
      msg: "Name is: {{ NAME }}"


#!.Args
 # 2.tasks
 #3.files
 #4.play